package game;

import physicsManager.PhysicalObject;

/**
 * Implement. Ship AI code should use this rather than test for individual implementations.
 * @author noise
 *
 */
public class Weapon {
	public boolean isValidTarget(PhysicalObject target) {
		return false;
//		if (!target.isAlive()) {
//			return false;
//		}
//		else if (position.add(systemBrick.getPosition()).distance(target.getPosition()) > maxRange) {
//			return false;
//		}
//		return true;
	}
}
